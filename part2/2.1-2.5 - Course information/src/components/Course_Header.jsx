// Responsible for creating the header section of a course.
const Course_Header = ({ courseName }) => {
  return (
    <div>
      <h1>{courseName}</h1>
    </div>
  );
};

export default Course_Header;
